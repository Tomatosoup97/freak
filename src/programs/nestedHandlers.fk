handle
    handle
        let x <- do Inc 1 in do Double x
    with {
        Inc p r -> let t <- return p + 1 in r t |
        return x -> return x
    }
with {
    Double p r -> let t <- return p * 2 in r t |
    return x -> return x
}

