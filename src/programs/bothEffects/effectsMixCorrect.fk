cohandle IO using () at
    handle
        observe Read "file"
    with {
        Choice p r -> return True |
        return x -> return x
    }
through {
    Read filename r -> r "contents" |
    return x -> return x
}
